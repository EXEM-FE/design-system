# Load NVM for npx (VSCode Source Control compatibility)
export NVM_DIR="$HOME/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"

echo "📝 커밋 메시지 검증 중..."
npx --no -- commitlint --edit $1
